// Local demo mode is used until these are filled in.
// For a live shared database, copy this file to config.js, include config.js
// in index.html instead of config.example.js, and paste your Supabase values.
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
